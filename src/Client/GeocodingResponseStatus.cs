namespace Velyo.Google.Services
{
    public enum GeocodingResponseStatus
    {
        None,
        OK,
        ZERO_RESULTS,
        OVER_QUERY_LIMIT,
        REQUEST_DENIED,
        INVALID_REQUEST
    }
}
